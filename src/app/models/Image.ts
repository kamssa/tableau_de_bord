export class Image {
  constructor(
    public id?: number,
    public version?: number,
    public idTerrain?: number,
    public nom?: string,
    public imageUrl?: string,
    public imageId?: string
  ) {
  }
}
